#!/usr/bin/env bash
#MISE description="Verify app binary has arm64 and x86_64 slices"
# Env overrides: CONFIGURATION (default Release)
set -eo pipefail

lipo ".build/DerivedData/Build/Products/${CONFIGURATION:-Release}/TidalSwift.app/Contents/MacOS/TidalSwift" -verify_arch arm64 x86_64
